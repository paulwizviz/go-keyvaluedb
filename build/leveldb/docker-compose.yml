version: '3.7'

services:

  leveldb:
    build:
      context: ../../
      dockerfile: ./build/leveldb/leveldb.dockerfile
      args:
        GO_VER: 1.22
        OS_VER: 3.19
    image: ${LEVELDB_CMD_IMAGE:-go-db/levedb:current}
